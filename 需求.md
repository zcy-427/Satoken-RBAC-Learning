# 基于 Sa-Token 的 RBAC 权限管理系统需求

> 文档状态：阶段性需求草案
> 更新时间：2026-09-10

## 1. 项目定位

本项目是一个以学习为主要目的的单租户权限管理系统，用于理解主流管理系统中的登录认证、RBAC 授权、数据权限和前后端权限协作。

项目应当能够完整运行并适合作为个人作品展示，同时保留基础的二次开发能力，但不以生产级企业平台为目标，不引入微服务、复杂运维和合规体系。

## 2. 建设目标

1. 掌握 Sa-Token 的登录认证、权限认证、角色校验、会话管理和踢人下线能力。
2. 实现用户、角色、权限、部门和岗位组成的 RBAC 权限模型。
3. 实现菜单、按钮、接口和数据四个层级的权限控制。
4. 使用一个轻量的项目与任务模块验证权限设计，而不是只完成后台配置页面。
5. 提前统一 API、数据格式、错误处理和权限编码规范。
6. 提供清晰的项目结构、初始化数据、说明文档和 Docker Compose 启动方式。

## 3. 技术栈

### 3.1 后端

- Java 21 LTS
- Spring Boot 4.0.8
- Sa-Token 1.46.x
- MyBatis-Plus
- MySQL
- Redis
- Flyway
- OpenAPI 3 / Swagger UI
- Maven

### 3.2 前端

- Vue 3
- TypeScript
- Vite
- Element Plus
- Pinia
- Vue Router
- Axios
- ESLint + Prettier

### 3.3 部署

- Docker Compose
- 默认包含前端、后端、MySQL 和 Redis
- Nginx 用于统一入口和前端静态资源服务
- 文件默认存储在本地目录，MinIO 作为可选演示组件

## 4. 架构约束

1. 后端采用模块化单体架构，不拆分微服务。
2. 模块之间通过明确的 Service 接口协作，禁止随意跨模块操作数据。
3. 建议划分为认证会话、系统权限、组织架构、项目任务、文件、日志和通用基础设施模块。
4. 系统按单租户实现，本阶段不开发租户管理和 SaaS 运营功能。

## 5. 核心功能

### 5.1 登录与账号

- 不开放用户自主注册，账号由管理员创建或批量导入。
- 第一版使用用户名和密码登录，预留手机号、邮箱等登录方式的扩展位置。
- 首次登录要求修改临时密码。
- 登录连续失败达到阈值后临时锁定账号，管理员可以解除锁定或重置密码。
- 禁用或删除用户、修改密码后，立即清除该用户的全部登录会话。
- 允许同一账号多设备登录，每个设备使用独立会话，默认最多保留 5 个有效设备。
- 用户可以查看并注销自己的在线设备，管理员可以强制指定会话或账号下线。
- 会话和登录状态存储在 Redis 中。
- 参考 Sa-Token 前后端分离模式，前端通过请求 Header 提交 Token；Token 名称和 Bearer 前缀在项目中统一配置，关闭 Cookie 读取。
- MFA、OAuth2、OIDC 和 SSO 本阶段不实现。

### 5.2 用户、部门与岗位

- 部门采用树形结构。
- 用户具有一个主部门，并可以关联多个兼任部门。
- 用户可以绑定多个岗位。
- 岗位用于组织和人员管理，本阶段不直接授予功能权限。
- 支持用户、部门和岗位的启用、禁用及基础查询。

### 5.3 角色与权限

- 用户可以绑定多个角色，功能权限取所有角色权限的并集。
- 数据权限取各角色可见数据集合的并集。
- 第一版不支持角色继承，提供角色复制能力。
- 权限编码使用稳定、独立的字符串，例如 `system:user:create`。
- 菜单和按钮绑定权限编码，后端接口使用 Sa-Token 强制校验。
- 接口权限由后端代码定义并同步为权限清单，管理页面只负责分配已有权限。
- 权限、角色、部门或账号状态发生变化后，相关权限缓存立即失效，下一次请求使用新权限。

### 5.4 权限层级

1. 菜单权限：决定用户能够访问哪些页面和路由。
2. 按钮权限：决定用户能够执行新增、修改、删除、导入和导出等操作。
3. 接口权限：后端必须独立校验，不能依赖前端隐藏按钮保证安全。
4. 数据权限：限制用户能够查看和操作的数据范围。

数据权限至少支持：

- 仅本人
- 本部门
- 本部门及下级部门
- 自定义部门
- 全部数据

数据权限默认根据用户的主部门计算。兼任部门仅用于人员关系和项目协作；只有角色明确配置自定义部门范围时，兼任部门才会影响数据权限。

### 5.5 超级管理员

- 系统内置超级管理员，默认拥有全部功能权限和数据权限。
- 超级管理员不依赖普通角色授权，账号不能删除。
- 超级管理员的操作仍然记录日志。
- 首次运行后必须修改默认密码。

## 6. 项目与任务演示模块

该模块用于验证真实业务中的权限控制，不扩展为完整项目管理产品。

### 6.1 项目

- 基本信息：名称、负责人、参与成员、所属部门、状态、开始时间和结束时间。
- 项目负责人可以维护项目成员和项目内任务。
- 普通成员可以查看自己负责或参与的项目。
- 支持跨部门添加项目成员。

### 6.2 任务

- 基本信息：所属项目、标题、执行人、优先级、截止时间、状态和说明。
- 状态流程：待处理、进行中、已完成、已关闭。
- 任务执行人可以更新自己任务的状态和进度。
- 支持基础评论和操作记录。

### 6.3 跨部门访问规则

- 项目负责人或参与成员可以访问对应项目，不受所属部门限制。
- 部门数据权限用于管理视角，不阻止合法的项目协作。
- 没有项目关系、任务关系或部门数据权限时拒绝访问。

## 7. API 规范

### 7.1 基本约定

- API 统一使用 `/api/v1` 前缀。
- 采用 RESTful 风格，复杂业务动作使用语义化动作接口。
- 正确使用 `GET`、`POST`、`PUT`、`PATCH` 和 `DELETE`。
- 参数必须进行校验，时间、枚举、分页和排序格式保持统一。
- 使用 OpenAPI 3 生成接口文档；Swagger UI 仅在开发和测试环境开放。

### 7.2 统一响应

```json
{
  "code": "SUCCESS",
  "message": "操作成功",
  "data": {},
  "traceId": "01J...",
  "timestamp": "2026-09-08T10:30:00+08:00"
}
```

分页响应中的 `data` 统一为：

```json
{
  "list": [],
  "page": 1,
  "size": 20,
  "total": 100,
  "pages": 5
}
```

Java 后端统一响应包装类命名为 `Result<T>`，使用 Lombok `@Data` 实现可变响应对象；不得改变上述 JSON 字段和语义：`code` 使用字符串，字段使用 `message`、`traceId` 和 `timestamp`。

后端错误码与默认提示统一由 `ErrorCode` 枚举管理，业务代码和全局异常处理器不得重复硬编码已定义的错误码。

### 7.3 HTTP 状态码

- `400`：参数错误
- `401`：未登录或登录状态失效
- `403`：没有权限
- `404`：资源不存在
- `409`：数据冲突
- `429`：请求过于频繁
- `500`：服务器内部异常

禁止所有请求无条件返回 HTTP 200。

## 8. 辅助功能

### 8.1 日志

- 记录登录成功、登录失败、退出和强制下线。
- 记录新增、修改、删除、授权、导入和导出等关键操作。
- 日志包含操作者、时间、IP、请求接口、执行结果和耗时。
- 密码、Token 等敏感数据禁止写入日志。
- 本阶段不实现复杂日志归档和合规留存策略。

### 8.2 文件管理

- 支持用户头像、项目附件和任务附件。
- 校验文件类型和大小，使用随机文件名保存。
- 下载文件时校验访问权限。
- 数据库存储文件元数据，文件内容默认保存在本地目录。
- MinIO 作为可选学习内容，不要求第一阶段必须启用。

### 8.3 数据字典与系统配置

- 数据字典用于管理状态、类型、优先级等可选项。
- 系统配置用于管理登录失败次数、文件大小限制等普通业务参数。
- 密码、密钥和数据库连接信息不得保存在普通系统配置表中。

### 8.4 导入与导出

- 支持用户等基础数据的批量导入。
- 支持列表数据导出，导出结果必须遵守数据权限。
- 导入和导出操作需要独立权限并记录日志。
- 第一阶段只实现常用数据量，不实现大规模异步导入平台。

### 8.5 首页看板

- 展示项目、任务、用户和登录等基础统计信息。
- 看板数据必须遵守当前用户的数据权限。
- 不引入完整 BI 系统。

## 9. 数据与并发处理

- 用户、部门、岗位、角色、项目和任务采用逻辑删除。
- 删除前检查有效关联，避免产生无效引用。
- 使用数据库唯一索引保证用户名、角色编码和权限编码等数据的唯一性。
- 项目和任务可使用乐观锁演示并发更新冲突处理。
- 新增和状态操作进行基础防重复提交，不建设通用幂等平台。

## 10. 测试要求

- 为权限计算、登录会话和任务状态流转编写单元测试。
- 为菜单、按钮、接口和数据权限编写关键集成测试。
- 使用真实 MySQL 和 Redis 验证必要的集成场景，可使用 Testcontainers 简化环境。
- 前端只覆盖关键组件和主要登录、授权流程。
- 不设置企业级覆盖率门禁，不在第一阶段开展完整性能测试。

## 11. 交付与启动要求

- 提供数据库迁移脚本和必要的演示数据。
- 提供默认管理员、普通用户及不同数据范围角色用于演示。
- 提供 README，说明架构、权限模型、启动步骤和测试账号。
- 提供 Docker Compose，一条命令启动主要依赖和应用。
- 提供 `.env.example`，不得提交真实密码和密钥。
- 提供基础 API 文档和权限验证示例。

## 12. 可选与待确认功能

以下功能不属于当前阶段的硬性需求：

- AI 功能：可以加入轻量的权限说明、操作日志摘要或项目任务助手，但必须先明确学习价值和实现成本。
- CRUD 代码生成器：可以作为后续开发辅助模块，当前尚未确认纳入。
- Spring Task：可以用于简单的任务到期提醒和临时文件清理，不建设可配置调度平台。
- 站内通知：可在项目与任务模块完成后再决定是否保留。

## 13. 明确不做

- 多租户和 SaaS 运营功能
- 微服务、服务注册中心和分布式事务
- Kubernetes
- OAuth2、OIDC 和单点登录
- 企业级监控、日志平台和告警中心
- 复杂工作流引擎
- 完整低代码平台
- 企业合规、长期日志归档和灾备体系

## 14. Git 与 GitHub 工作流

- 项目使用 Git 进行版本管理，并建立公开的 GitHub 仓库。
- 每个目标或功能使用独立分支开发，分支名称应能表达功能用途。
- 每完成一个可独立验证的目标或功能，必须创建一次原子提交并推送到 GitHub。
- 提交前至少完成与本次改动相关的构建、检查或测试，禁止主动提交已知无法编译的代码。
- 提交信息采用 Conventional Commits 风格，例如 `feat:`、`fix:`、`docs:`、`test:`、`refactor:` 和 `chore:`。
- 提交信息的类型必须使用英文，冒号后的具体描述必须使用中文，例如 `feat: 创建RBAC基础数据表`。
- 提交前检查变更范围，不混入与当前目标无关的文件。
- 禁止提交真实密码、Token、密钥、`.env`、运行日志、上传文件和本地构建产物。
- 为环境变量提供 `.env.example`，其中只包含安全的示例值或占位符。

## 15. 协作与需求同步约定

- `需求.md` 是当前项目的需求基准；开始新的功能或目标前，先核对与本次工作相关的需求条目。
- 用户确认新的功能、技术约束或协作方式后，应先更新本文件，再开始实现或提交代码。
- 后端代码由用户根据引导编写；助手负责拆分任务、解释设计、检查代码、执行验证并指出问题，不直接代写后端业务代码。
- 前端代码由助手负责编写、验证和提交。
- 每完成一个目标，都应记录验证结果、提交记录和下一步目标，避免需求、代码和 Git 历史不一致。
